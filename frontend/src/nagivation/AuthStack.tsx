import React from "react";
import { Text, View } from "react-native";

interface AuthStackProps {}

const AuthStack: React.FC<AuthStackProps> = ({}) => {
  return (
    <View>
      <Text>Placeholder App Stack</Text>
    </View>
  );
};

export default AuthStack;
